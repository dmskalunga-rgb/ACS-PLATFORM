# ACS-AIGOV M0B AI Risk Governance local qualification evidence

Status: `LOCAL_QUALIFICATION_COMPLETE` (subject to separate human publication disposition).

Execution date: `2026-09-23`. Isolated worktree: `D:\ACS-AIGOV-M0B-IMPLEMENTATION`, branch `codex/aigov-m0b-ai-risk-governance`, base `f7c84654ce8ea4b05323809ee3256070e6048f22`. This record covers uncommitted M0B source and a fresh disposable PostgreSQL 17 database. No persistent ACS database, production deployment, model/provider runtime, staging, commit, push, PR or merge was used.

This document supplements, but does not amend, the approved AIGCP G0 governance package. Unresolved operational scoring bands and exact MPA values for protected residual-risk decisions remain unresolved. Protected acceptance/rejection fails closed as `NOT_AUTHORIZED_PENDING_POLICY`; a proposed treatment is not an approved decision.

## Implementation and authority boundary

M0B adds tenant-bound AI risk identity, immutable owner, assessed inherent/residual scores, proposed treatments, residual reviews and monitoring history. Assessments and other history are append-only. A tenant-scoped reference may point to an M0A inventory entity and a typed XCAP-005 evidence record, but M0B does not copy raw evidence or create a parallel inventory, evidence, framework, Event Foundation, AuthorizationPort or MPA authority. Framework-control mapping remains with governed XCF-M2; future enterprise GRC projection/export is not implemented. No AI model/provider executes.

The service binds a signed-OIDC actor to canonical active membership and server-issued tenant context. `AuthorizationPort` authorizes the requested operation; the PostgreSQL repository rechecks the trusted actor/tenant/action. The M0B runtime role has least-privilege grants, with RLS and FORCE RLS on all six M0B relations. The idempotency-result relation is not readable through the general risk-read action.

The approved inherent formula is `L × I × ((E + D + A + B) / 4)` and residual is `IR × (1 − C)`. Integer arithmetic in the repository reproduces PostgreSQL half-up rounding to two decimals, including an executed tie case (`0.575 → 0.58`). No operational rating band is activated.

## Disposable PostgreSQL and behavioral evidence

`corepack pnpm db:validate` passed against a fresh disposable PostgreSQL 17 database. The canonical validator applied the M0B forward migration after the existing baseline, verified schema/roles/grants and RLS/FORCE RLS, rolled M0B back cleanly, then reapplied it. Historical migrations were not modified. The validator also confirmed existing trusted-context, M0A, XCAP-005, XCAP-011, XCF-M1, MPA and Event Foundation validation paths.

The final focused production-path run, `corepack pnpm exec vitest run services/platform-api/src/ai-risk.database.e2e.test.ts services/platform-api/src/ai-risk.http.e2e.test.ts`, passed `12/12` across two files. The tests exercised signed OIDC and real HTTP/PostgreSQL, owner and tenant binding, append-only history, score/reassessment rounding, canonical authorization, XCAP-005/M0A tenant-scoped references, stale-version and exactly-one-wins concurrency, same-key idempotent replay and different-command conflict, cross-tenant key isolation, denied direct DB bypass, missing/forged context, owner spoofing, mass assignment and protected-decision fail-closed behavior. In fault injection, state, idempotency result, denied/allowed audit and tenant-safe transactional outbox rolled back coherently. A refused protected decision created denied audit evidence and no completion event.

Service-focused tests passed `5/5`. The Platform API unit suite passed `189` with `3` configured skips. The cross-capability M0B/M0A/XCF focused run passed `36/36`; Event Foundation E2E `5/5`, XCAP-005 real PostgreSQL E2E `26/26`, and XCAP-011 database plus HTTP E2E `14/14` passed. The standalone Web unit suite passed `160/160`.

## Raw regression history and attribution limit

The first aggregate `corepack pnpm test` run **failed** when a Web Vitest worker did not start in time; this raw failure is preserved. The focused Web auth-provider suite then passed `16/16` and the unchanged standalone Web suite passed `160/160`. A second aggregate run **failed** with one Platform API `beforeAll` hook timeout and four existing HTTP-test timeouts while the local Docker build was active (`176 PASS / 4 FAIL / 12 SKIP` in Platform API). Those three existing HTTP files passed `17/17` in an unchanged focused rerun after the image build ended. A third, unchanged canonical aggregate run then passed `11/11` Turbo tasks, including Platform API `189 PASS / 3 configured SKIP` and Web `160/160`. Neither the two failed raw runs nor their timeouts are converted to PASS; the terminal serial rerun is separately recorded as PASS.

The first M0B Phase 1 canonical E2E run **failed** with `337 PASS / 35 FAIL`; the first Contract signed-OIDC mutation received HTTP 500 `FOUNDATION_INTERNAL_ERROR`. A clean checkout of the exact current-develop base, on a fresh disposable PostgreSQL 17 database, passed the same canonical suite `372/372`. A second M0B run with a fresh disposable PostgreSQL 17 database and the same process bindings also passed `372/372`. No Contract, Subscription, timeout or expectation was changed between M0B runs. Thus the 35 failures cannot be labeled pre-existing current-develop failures, but no deterministic M0B-caused regression was reproduced. The exact cause of the initial HTTP 500 remains undetermined. The raw failed run is not rewritten as PASS.

## Static and local security evidence

Frozen-lockfile install completed `407/407`; manifests and lockfile remained unchanged. Final current-tree `corepack pnpm format:check`, lint (`11/11` Turbo tasks), typecheck (`11/11`) and build (`7/7`) passed after the score arithmetic change. Parallel first attempts at lint/typecheck/build had failed due local Turbo cache-log write collisions; their unchanged serial reruns passed. `git diff --check` passed. The M0B traceability matrix is at `docs/traceability/ACS-AIGOV-M0B-IMPLEMENTATION-TRACEABILITY-MATRIX.md`.

`corepack pnpm audit --audit-level high` exited `0`: `0 HIGH`, `0 CRITICAL`, `4 MODERATE` remaining unsuppressed. The CycloneDX 1.7 lockfile SBOM contained `461` components (`388992` bytes); its generated artifact was verified and removed, not added to the changeset. Local Trivy filesystem vulnerability/IaC scanning, excluding generated `node_modules` and `.git` like the pre-install CI scope, exited `0` with `0 HIGH/CRITICAL` findings. The separate local Trivy secret scan exited `0` with no findings reported. A first broad filesystem scan timed out while traversing generated Windows dependency links; it did not establish a security result and is preserved as an environmental raw failure. No CVE waiver, scanner suppression or threshold weakening was made.

The current M0B Platform API image built successfully as `acs/platform-api:aigov-m0b-local` (`sha256:9e01494c3aa14fd4f4cd6bdbb3bf7927da2a34c7a2a082e83a983162c3a39805`). Local Trivy 0.69.3 scanned that exact image with an updated official vulnerability database, exit `0`, reporting `0 HIGH/CRITICAL` for Alpine 3.24.1 and its Node packages. The default DB mirror stalled after reporting 100% transfer and did not yield a scan result; the alternate official `ghcr.io/aquasecurity/trivy-db:2` completed without changing severity thresholds. Web application source and Dockerfile were unchanged; a separate M0B Web image scan was not executed and is not claimed. The unchanged Web runtime/base was scanned during M0A qualification, and the current M0B lockfile/IaC scan covers the changed-contract dependency surface. Local CodeQL and remote mandatory CI are not claimed; publication is not authorized.

## Acceptance, traceability and remaining gate

The M0B traceability matrix maps eleven applicable requirement/boundary entries. Applicable local acceptance evidence covers identity, authorization, tenant isolation, RLS/FORCE RLS, append-only history, scoring, concurrency, idempotency, atomic audit/outbox, XCAP-005 references and cross-capability regression. `ACC-018` protected-decision success is **blocked by approved unresolved G0 policy values**, not marked PASS or waived. `ACC-023` remote CI is not applicable to this unpublished local worktree. Control mapping through XCF-M2 and enterprise GRC projection are outside M0B authority.

At final local custody, `develop` remained at the expected base, the isolated M0B branch/HEAD were unchanged, the staging area was empty, only the sixteen authorized M0B implementation/test/evidence paths were changed, and `git diff --check` passed. `KNOWN_MATERIAL_M0B_DEFECTS = NONE` from executed evidence. Policy-value and remote-publication gates are separately governed future actions, not local M0B qualification blockers. `STAGING = EMPTY`; `COMMIT = NONE`; `PUSH = NONE`; `PR = NONE`; `MERGE = NONE`; `DEPLOYMENT = NONE`.
