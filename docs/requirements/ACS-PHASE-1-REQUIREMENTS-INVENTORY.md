# ACS Phase 1 Requirements Inventory

Status: `IMPLEMENTATION_DEFINED`

Scope: Phase 1 — Platform Foundation and Multi-Tenancy

No identifier in this inventory is a new normative `ACS-REQ`. Stable baseline volume and
section references are used because individual requirement identifiers are absent.

| Baseline reference | Requirement statement                                                                                  | Implementation implication                                                           | Security implication                     | Data implication                                   | API implication                             | Testing implication                  | Evidence requirement                 | Status                    |
| ------------------ | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------ | ---------------------------------------- | -------------------------------------------------- | ------------------------------------------- | ------------------------------------ | ------------------------------------ | ------------------------- |
| VOL-I-1.4          | Every functional slice integrates data, authentication, authorization, audit, observability, and tests | Deliver one complete current-tenant context slice                                    | Fail closed at every boundary            | Persist only justified platform entities           | Contract-first endpoint                     | Unit, API, DB, security, E2E         | Code, test, CI, and runtime evidence | `BASELINE_DEFINED`        |
| VOL-I-1.4/1.6      | Multi-tenancy, RLS, least privilege, and tenant isolation are mandatory                                | Treat tenant context as a security boundary                                          | Never trust client tenant selection      | Every tenant-owned row is RLS protected            | Tenant resolved server-side                 | Intentional cross-tenant abuse tests | RLS and tenant-escape evidence       | `BASELINE_DEFINED`        |
| VOL-II-2.1/2.3     | ACS is multi-tenant by design and tenant is canonical information                                      | Establish canonical tenant identity                                                  | Stable UUID independent of display name  | `tenants` is the source of truth                   | Return authorized current tenant only       | Identity and isolation tests         | ADR and data-model evidence          | `DERIVED_FROM_BASELINE`   |
| VOL-II-2.7         | Identity, authorization, audit, event, and observability are transverse services                       | Extend accepted Phase 0 ports, not endpoint-local policy                             | Central fail-closed boundaries           | User and membership primitives only                | Shared platform context contract            | Port and integration tests           | ADR-0011 and implementation evidence | `DERIVED_FROM_BASELINE`   |
| VOL-VI-6.1-6.3     | Single source, domain ownership, RLS, audit, constraints, indexes, and justified fields                | Use SQL-first migrations and explicit roles                                          | FORCE RLS and least privilege            | UUID keys, status, timestamps, constrained slug    | No generic CRUD                             | Migration and constraint tests       | Migration hash and DB validation     | `BASELINE_DEFINED`        |
| VOL-VI-6.4.1       | Platform Foundation names tenants, users, memberships, roles, permissions, and audit logs              | Implement only tenants, users, memberships, and audit logs needed by the first slice | Avoid premature full IAM/RBAC            | Four canonical tables in schema `platform`         | Current context retrieval only              | Entity and relationship tests        | Schema dictionary and SQL evidence   | `BASELINE_DEFINED`        |
| VOL-VI-6.5         | All multi-tenant tables require RLS; privileged functions are minimized and audited                    | Separate resolver and tenant application roles                                       | No default superuser/service-role access | RLS policies use transaction-local trusted context | Repository opens scoped transaction         | Missing/invalid/cross-tenant tests   | Direct database evidence             | `BASELINE_DEFINED`        |
| VOL-VII-7.3        | Services validate input, tenant, permission, transaction, audit, events, and normalized response       | Layer request → identity → resolver → authorization → repository                     | Prevent confused deputy and BOLA         | Resolve active membership before tenant data       | `/api/v1/platform/context`                  | API success and denial tests         | OpenAPI and audit evidence           | `BASELINE_DEFINED`        |
| VOL-VII-7.4        | APIs require `/api/v1`, schemas, authn/z, tracing, correlation, normalized errors, and contract tests  | Extend Fastify shell with a documented endpoint                                      | Do not leak cross-tenant distinctions    | Read-only query in scoped transaction              | GET current context                         | Contract and negative tests          | OpenAPI artifact                     | `BASELINE_DEFINED`        |
| VOL-VII-7.7        | Events carry version, tenant, correlation, causation, producer, classification, and integrity          | Emit no event for a read-only context query                                          | Prevent speculative/spoofed events       | No outbox until a durable mutation exists          | Not applicable to first read slice          | Assert no event is emitted           | Explicit N/A decision in evidence    | `IMPLEMENTATION_DECISION` |
| VOL-VII-7.10       | Propagate request, correlation, tenant, user, service, and operation context                           | Controlled structured logs and traces                                                | No tenant/user metric labels or secrets  | Audit record carries safe identifiers              | Response preserves correlation              | Redaction and correlation tests      | Observability test evidence          | `BASELINE_DEFINED`        |
| VOL-VIII-8.1-8.4   | Phase 1, global DoD, and QG-01 through QG-12 apply as defined                                          | Maintain traceability and evidence continuously                                      | Security and QG-04 block promotion       | Migration/RLS evidence mandatory                   | API and UX gates apply to implemented slice | Full regression and E2E              | Phase 1 evidence directory and CI    | `BASELINE_DEFINED`        |

## Explicit implementation decisions

- Tenant lifecycle is limited to `ACTIVE` and `INACTIVE`. This is the minimum mechanism
  needed to deny an inactive tenant or membership; no lifecycle mutation API is authorized.
- Tenant slug is globally unique as an operator-friendly stable key; canonical security
  identity remains the UUID.
- The first endpoint is read-only. No domain event or transactional outbox is justified.
- A development/test identity adapter may accept a subject header only outside staging and
  production. Production uses the explicit provider-neutral `oidc` mode and fails startup when
  issuer, audience, HTTPS JWKS URI, or algorithm policy is missing or invalid.
- The accepted browser boundary receives an access token from an external Authorization Code +
  PKCE/BFF broker in memory. This slice does not implement an IdP login client or persist bearer
  tokens; provider session revocation remains deployment-specific.
