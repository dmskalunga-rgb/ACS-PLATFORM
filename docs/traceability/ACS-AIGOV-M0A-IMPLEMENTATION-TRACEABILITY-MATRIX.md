# ACS-AIGOV M0A implementation traceability matrix

Status: `LOCAL_IMPLEMENTATION_EVIDENCE`; publication and remote CI are not authorized.

This is an M0A-only evidence overlay on the approved AIGCP requirements and acceptance matrix. It does not change either governance document. An applicable row is complete only for the authorized AI Inventory operation shown here; future AIGOV slices retain their own obligations.

| Requirement | Architecture / authority                            | M0A implementation                                                                                              | Executed test / evidence                                                               | M0A acceptance                                                                                           |
| ----------- | --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| REQ-001     | Canonical trusted tenant context; active membership | `AiInventoryService`, `PostgresAiInventoryRepository`, `ai_governance.ai_systems`                               | Signed-OIDC HTTP `3/3`; repository `11/11`; canonical DB validation                    | PASS                                                                                                     |
| REQ-002     | XCAP-005 provenance; model identity boundary        | `models`, immutable `model_versions`; SHA-256 and tenant-bound XCAP-005 reference                               | Contract/service `11/11`; repository `11/11`; XCAP-005 E2E `26/26`                     | PASS for M0A identity/hash/reference; signature verification and model artifact trust remain outside M0A |
| REQ-003     | XCAP-005; dataset metadata authority                | `datasets`, immutable `dataset_versions`; classification, permitted uses, residency/retention policy references | Repository `11/11`; canonical DB RLS/FORCE RLS and cross-tenant FK proof               | PASS for M0A metadata; no raw dataset store                                                              |
| REQ-008     | Server tenant authority; RLS/FORCE RLS              | All seven tenant-owned inventory relations and command results                                                  | Fresh PostgreSQL 17 `db:validate`; real repository and HTTP tenant-negative tests      | PASS                                                                                                     |
| REQ-009     | Canonical ACS authorization boundary                | OIDC identity, active membership, server context, AuthorizationPort, repository context assertion               | Service `6/6`; repository forged actor/context denial; real signed-OIDC HTTP `3/3`     | PASS                                                                                                     |
| REQ-010     | AuthorizationPort                                   | Permission-specific `AiInventoryService.actor` gate; no parallel policy engine                                  | Service denial tests; HTTP denial and missing-context tests                            | PASS                                                                                                     |
| REQ-012     | Prompt/version registry                             | `prompts`, immutable `prompt_versions`, SHA-256 and reference only                                              | Contracts; repository `11/11`; immutable version SQLSTATE `42501`/`55000`              | PASS for M0A registry; promotion/security lifecycle outside M0A                                          |
| REQ-022     | Event Foundation transactional outbox               | Domain mutation, bounded audit, outbox, command result in one transaction                                       | Repository `11/11`, including five injected failure points; Event Foundation E2E `5/5` | PASS                                                                                                     |
| REQ-025     | Classification and sensitive-data minimization      | Strict request schemas; metadata-only audit/event; no raw prompt, dataset, model or evidence payload            | Contract negative tests; HTTP mass-assignment denial; local secret scan `0`            | PASS                                                                                                     |
| REQ-026     | Existing concurrency patterns                       | Expected-version update; tenant-scoped idempotency; transactional rollback                                      | Repository `11/11`: stale-version, exactly-one-wins race, replay and failure injection | PASS                                                                                                     |
| REQ-027     | Governance state representation                     | Registered-only M0A state and bounded response envelopes                                                        | Contracts `5/5`; HTTP `4/4` plus real HTTP `3/3`                                       | PASS for M0A registration; future lifecycle states outside M0A                                           |

`M0A_APPLICABLE_REQUIREMENTS = 11`

`M0A_APPLICABLE_COMPLETE = 11`

`M0A_APPLICABLE_COMPLETENESS = 100%`

REQ-004–007, 011, 013–021 (except the rows above), 023 and 024 govern future AI-risk, evaluation, decision, inference, provider, kill-switch, MPA-protected or interoperability surfaces not implemented by M0A. M0A does not claim their runtime acceptance. REQ-002 model signature validation is specifically deferred because M0A registers metadata and does not acquire or execute model artifacts or define a new trust root. The M0A evidence reference remains a tenant-bound link to canonical XCAP-005, not a substitute evidence authority.

For executed gate details, raw failures, security findings and publication boundary, see `docs/evidence/aigov/ACS-AIGOV-M0A-IMPLEMENTATION-QUALIFICATION-EVIDENCE.md`.
